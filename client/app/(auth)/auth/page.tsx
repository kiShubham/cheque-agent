import { AuthenticationCard } from "@/container/auth/auth";
import React from "react";

const page = () => {
  return (
    <div>
      <AuthenticationCard />
    </div>
  );
};

export default page;
